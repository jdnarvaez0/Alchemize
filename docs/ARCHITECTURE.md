# ⚗️ Arquitectura de Alchemize

## Diagrama del Laboratorio Alquímico

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                           ⚗️ CALDERO OBSIDIAN ⚗️                              ║
║  ┌───────────────────────────────────────────────────────────────────────┐   ║
║  │                      PLUGIN ALCHEMIZE                                  │   ║
║  │                                                                        │   ║
║  │  ┌────────────────────────────────────────────────────────────────┐   │   ║
║  │  │                    INTERFAZ DE VISIÓN                           │   │   ║
║  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │   │   ║
║  │  │  │ PreviewModal │  │ SettingsTab  │  │ Command Palette      │  │   │   ║
║  │  │  │ (Cristal)    │  │ (Grimorio)   │  │ (Invocaciones)       │  │   │   ║
║  │  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │   │   ║
║  │  └────────────────────────────────────────────────────────────────┘   │   ║
║  │                                                                        │   ║
║  │  ┌────────────────────────────────────────────────────────────────┐   │   ║
║  │  │                    NÚCLEO ALQUÍMICO                             │   │   ║
║  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │   │   ║
║  │  │  │  Alchemize   │  │ ImageHandler │  │ Converter Engine     │  │   │   ║
║  │  │  │  (main.ts)   │  │ (Captura     │  │ (Transmutación)      │  │   │   ║
║  │  │  │              │  │  de esencias)│  │                      │  │   │   ║
║  │  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │   │   ║
║  │  └────────────────────────────────────────────────────────────────┘   │   ║
║  │                                                                        │   ║
║  │  ┌────────────────────────────────────────────────────────────────┐   │   ║
║  │  │                 GRIMORIO DE FÓRMULAS (Strategy Pattern)         │   │   ║
║  │  │                                                                  │   │   ║
║  │  │   ┌─────────────────┐      ┌──────────────────────────────┐     │   │   ║
║  │  │   │ ExtractorRegistry│────▶│   BaseExtractor (abstract)   │     │   │   ║
║  │  │   │   (El Grimorio)  │      │                              │     │   │   ║
║  │  │   └─────────────────┘      │   • canHandle(url, doc)       │     │   │   ║
║  │  │                            │   • extract(doc)              │     │   │   ║
║  │  │   ┌─────────────────────────────────────────────────────┐   │     │   │   ║
║  │  │   │                    FÓRMULAS ESPECIALIZADAS           │   │     │   │   ║
║  │  │   │  ┌────────────┐ ┌────────────┐ ┌────────────┐      │   │     │   │   ║
║  │  │   │  │  Generic   │ │    AWS     │ │   Medium   │ ...  │   │     │   │   ║
║  │  │   │  │(Universal) │ │SkillBuilder│ │            │      │   │     │   │   ║
║  │  │   │  └────────────┘ └────────────┘ └────────────┘      │   │     │   │   ║
║  │  │   └─────────────────────────────────────────────────────┘   │     │   │   ║
║  │  │                                                              │     │   │   ║
║  │  └────────────────────────────────────────────────────────────────┘   │   ║
║  │                                                                        │   ║
║  │  ┌────────────────────────────────────────────────────────────────┐   │   ║
║  │  │              COMPONENTES EXTERNOS (Incrustados)                 │   │   ║
║  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │   │   ║
║  │  │  │   Turndown   │  │ Readability  │  │   Obsidian API       │  │   │   ║
║  │  │  │(Transmutador)│  │(Extractor    │  │   (Interfaz Caldera) │  │   │   ║
║  │  │  │              │  │  de esencias)│  │                      │  │   │   ║
║  │  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │   │   ║
║  │  └────────────────────────────────────────────────────────────────┘   │   ║
║  │                                                                        │   ║
║  └───────────────────────────────────────────────────────────────────────┘   ║
║                                                                              ║
║  ┌───────────────────────────────────────────────────────────────────────┐   ║
║  │                         GRIMORIO PERSONAL                            │   ║
║  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐         │   ║
║  │  │   Notas      │  │    Assets    │  │     Configuración    │         │   ║
║  │  │   (.md)      │  │   (esencias) │  │     (data.json)      │         │   ║
║  │  └──────────────┘  └──────────────┘  └──────────────────────┘         │   ║
║  └───────────────────────────────────────────────────────────────────────┘   ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

## Flujo de Transmutación

```
El Aprendiz copia HTML
         │
         ▼
┌──────────────────────┐
│  Clipboard API       │  ← El caldero recibe la materia prima
└──────────────────────┘
         │
         ▼
┌──────────────────────┐
│  Smart Transmute     │  ← Detecta la naturaleza del contenido
│  (detectContentType) │
└──────────────────────┘
         │
         ├─── URL ───────▶ [Extraer HTML] ──▶ [Procesar]
         │
         ├─── HTML ──────▶ [DOMParser] ─────▶ [Crear Document]
         │
         └─── Markdown ──▶ [Procesar directamente]
                          │
                          ▼
                   ┌──────────────────────┐
                   │ ExtractorRegistry    │  ← Busca la fórmula adecuada
                   │ (findExtractor)      │
                   └──────────────────────┘
                          │
                          ▼
                   ┌──────────────────────┐
                   │  BaseExtractor       │  ← Aplica la fórmula
                   │  (Implementation)    │
                   └──────────────────────┘
                          │
                          ▼
                   ┌──────────────────────┐
                   │  Proceso Alquímico   │
                   │  • Readability       │
                   │  • Turndown          │
                   │  • Extracción        │
                   └──────────────────────┘
                          │
                          ▼
                   ┌──────────────────────┐
                   │  ExtractedContent    │
                   │  {                   │
                   │    title,            │
                   │    markdown,         │
                   │    images[],         │
                   │    metadata          │
                   │  }                   │
                   └──────────────────────┘
                          │
              ┌───────────┴───────────┐
              │                       │
              ▼                       ▼
       [Previsualizar]          [Cristalizar Directo]
              │                       │
              ▼                       │
       ┌──────────────┐              │
       │ PreviewModal │              │
       │ (Observar)   │              │
       └──────────────┘              │
              │                       │
              └───────────┬───────────┘
                          ▼
                   ┌──────────────────────┐
                   │ ImageHandler         │  ← Captura esencias visuales
                   │ (Process assets)     │
                   └──────────────────────┘
                          │
                          ▼
                   ┌──────────────────────┐
                   │ Obsidian Vault API   │  ← Cristaliza en el grimorio
                   │ (Persistencia)       │
                   └──────────────────────┘
                          │
                          ▼
                   ┌──────────────────────┐
                   │ Archivo .md creado   │  ← El oro puro
                   └──────────────────────┘
```

## Patrones de Diseño Alquímicos

### 1. Strategy Pattern (Fórmulas)

Cada fuente de conocimiento tiene su propia fórmula de transmutación:

```typescript
// La interfaz de toda fórmula
interface BaseExtractor {
    canHandle(url, doc): boolean;  // ¿Puede transmutar esto?
    extract(doc): Promise<ExtractedContent>;  // La transmutación
}

// Fórmulas específicas
class GenericExtractor implements BaseExtractor { }
class AWSSkillBuilderExtractor implements BaseExtractor { }
class MediumExtractor implements BaseExtractor { }
```

### 2. Registry Pattern (El Grimorio)

El grimorio mantiene todas las fórmulas disponibles:

```typescript
class ExtractorRegistry {
    register(extractor: BaseExtractor): void;  // Agregar fórmula
    findExtractor(url, doc): BaseExtractor;    // Buscar fórmula
}
```

### 3. Template Method Pattern

La clase base define el ritual, las subclases implementan los detalles:

```typescript
abstract class BaseExtractor {
    async extract(doc): Promise<ExtractedContent> {
        const cleanDoc = this.cleanHTML(doc);      // Ritual común
        const content = await this.parse(cleanDoc); // Abstracto
        return this.format(content);               // Ritual común
    }
    
    protected abstract parse(doc): Promise<string>; // Cada fórmula implementa
}
```

## Estructura del Grimorio (Archivos)

```
alchemize/
├── 📄 Archivos de Configuración
│   ├── manifest.json          # Identidad del artefacto
│   ├── package.json           # Componentes y dependencias
│   └── tsconfig.json          # Configuración de forja
│
├── 📄 Documentación del Saber
│   ├── README.md              # Grimorio público
│   ├── SKILL.md               # Conocimiento profundo
│   ├── ARCHITECTURE.md        # Planos del laboratorio
│   └── CONTRIBUTING.md        # Guía para aprendices
│
├── ⚗️ Código Fuente (src/)
│   ├── main.ts               # El caldero principal
│   ├── extractors/           # Las fórmulas de transmutación
│   │   ├── base.ts          # Base de toda alquimia
│   │   ├── registry.ts      # El grimorio
│   │   ├── generic.ts       # Fórmula universal
│   │   └── aws-skill-builder.ts  # Fórmula AWS
│   ├── core/                 # Esencias fundamentales
│   │   └── image-handler.ts # Captura de esencias visuales
│   └── ui/                   # Interfaces de visión
│       └── preview-modal.ts # Cristal de previsualización
│
└── 📄 Artefactos Generados
    ├── main.js               # Elixir final
    └── styles.css            # Vestimenta visual
```

## Decisiones Alquímicas (Arquitectura)

### ¿Por qué Sin Backend?
- **Simplicidad**: Todo ocurre en el caldero local
- **Offline**: Funciona sin conexión después de instalado
- **Sin costos**: No requiere mantener servidor
- **Clipboard-based**: Evita problemas de CORS

### ¿Por qué Registry de Extractores?
- **Extensibilidad**: Fácil agregar nuevas fórmulas
- **Separación**: Cada fuente tiene su propio grimorio
- **Testeabilidad**: Cada fórmula se prueba aisladamente

### ¿Por qué Turndown + Readability?
- **Combinación probada**: Usadas en producción por muchos
- **Flexibilidad**: Turndown permite reglas personalizadas
- **Robustez**: Readability maneja la mayoría de sitios

## Métricas del Laboratorio

| Aspecto | Valor | Nota |
|---------|-------|------|
| Tamaño del artefacto | ~160KB | Bundle completo |
| Tiempo de transmutación | < 1s | Para contenido típico |
| Esencias procesables | Ilimitado | Limitado por disco |
| Materia prima máxima | ~10MB | Límite práctico del clipboard |

## Seguridad Alquímica

- Todo el procesamiento es local en tu caldero
- No se envían datos a servidores externos
- Las esencias visuales se guardan directamente en tu grimorio
- No se ejecuta JavaScript del HTML original (DOMParser es seguro)

---

**⚗️ Transmuta el conocimiento. Transforma tu aprendizaje.**
